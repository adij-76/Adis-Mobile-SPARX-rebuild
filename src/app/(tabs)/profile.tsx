import { TabPlaceholder } from '@/components/tab-placeholder';

export default function ProfileScreen() {
  return (
    <TabPlaceholder
      title="Profile"
      icon="person"
      subtitle="Manage your account, goals and notification settings."
    />
  );
}
