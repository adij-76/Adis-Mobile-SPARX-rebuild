import { TabPlaceholder } from '@/components/tab-placeholder';

export default function CommunityScreen() {
  return (
    <TabPlaceholder
      title="Community"
      icon="people"
      subtitle="Connect with peers and join group conversations."
    />
  );
}
