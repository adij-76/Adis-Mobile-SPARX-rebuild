import { TabPlaceholder } from '@/components/tab-placeholder';

export default function DataScreen() {
  return (
    <TabPlaceholder
      title="My Data"
      icon="stats-chart"
      subtitle="Track your progress, streaks and check-ins over time."
    />
  );
}
